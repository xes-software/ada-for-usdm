import type { NextApiRequest, NextApiResponse } from "next";
import { signData, SignedMessage } from "@lucid-evolution/lucid";

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse,
) {}
